Completes the fields in a timesheet

export TS_PROJECT="project-name" && export TS_ROLE="your-role" && export TS_CLIENT="the-client" && export TS_PROJECT="the-project" && export TS_NAME="your name" && export TS_START_DATE="last monday" && php ./rp-timesheet.php

the file timesheet.xlsx will be created.
